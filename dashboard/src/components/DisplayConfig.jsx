export function DisplayConfigSection({ display }) {
    return (
        <article>
            <header>
                <h3>Display</h3>
                <p>Display hardware configuration</p>
            </header>

            <p>
                No display hardware configured.
            </p>
        </article>
    );
}
